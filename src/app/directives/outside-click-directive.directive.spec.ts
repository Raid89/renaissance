import { OutsideClickDirectiveDirective } from './outside-click-directive.directive';

describe('OutsideClickDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OutsideClickDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
