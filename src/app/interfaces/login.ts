export interface loginRequest {
    user: string;
    password: string;
}

export interface loginRequestDemo {
    type: number;
    params : {
        user: string;
        password: string;
    }  

}